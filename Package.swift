// swift-tools-version:4.0

import PackageDescription

let package = Package(
    name: "emoji_mapping",
    dependencies: [
    ],
    targets: [
        .target(
            name: "emoji_mapping",
            dependencies: []),
    ]
)
